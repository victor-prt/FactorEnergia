# yii2-app — FactorEnergia

Aplicación mínima Yii2 para la Parte 3 (API ERSE) y el refactor de facturación (Parte 2.2).

Instalación y Docker: ver el [README del repositorio](../README.md).

Comandos útiles:

```bash
composer install
php yii migrate --interactive=0
```
